public enum Action { Attack, Jump, Interact, Block }
public enum Movement { Horizontal, Vertical }
public enum Navigation { Horizontal, Vertical }
public enum UIAction { OK, Back, Details, Close, OpenMenu }
